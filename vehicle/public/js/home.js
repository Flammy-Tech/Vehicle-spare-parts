document.addEventListener('DOMContentLoaded', function() {
    function setInputIcon() {
        var input = document.getElementById('username');
        input.value = '\uf007';
    }

    setInputIcon();

});
